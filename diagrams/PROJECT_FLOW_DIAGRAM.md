# 🎯 Food-101 프로젝트 상세 흐름도

## 📋 목차
1. [전체 시스템 아키텍처](#1-전체-시스템-아키텍처)
2. [모델 훈련 파이프라인](#2-모델-훈련-파이프라인)
3. [API 서버 시작 과정](#3-api-서버-시작-과정)
4. [사용자 요청 처리 흐름](#4-사용자-요청-처리-흐름)
5. [프론트엔드 상호작용](#5-프론트엔드-상호작용)
6. [데이터 변환 체인](#6-데이터-변환-체인)

---

## 1. 전체 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│                      프로젝트 전체 구조                          │
└─────────────────────────────────────────────────────────────────┘

          오프라인 훈련 단계                온라인 서비스 단계
    ═══════════════════════════    ═══════════════════════════════

    ┌─────────────────┐           ┌──────────────────────────────┐
    │   Kaggle API    │           │      사용자 브라우저          │
    └────────┬────────┘           │   http://localhost:3000       │
             │                     │                              │
             ▼                     │  ┌────────────────────────┐  │
    ┌─────────────────┐           │  │   React Frontend       │  │
    │  Food-101 Data  │           │  │  • 이미지 업로드        │  │
    │  101,000 images │           │  │  • 결과 시각화         │  │
    └────────┬────────┘           │  │  • 모드 선택           │  │
             │                     │  └──────────┬─────────────┘  │
             ▼                     └─────────────┼────────────────┘
    ┌─────────────────┐                         │
    │  Data Loader    │                         │ HTTP POST
    │  • Transform    │                         │ (multipart/form-data)
    │  • Augmentation │                         │
    └────────┬────────┘                         ▼
             │                     ┌──────────────────────────────┐
             ▼                     │    FastAPI Backend Server    │
    ┌─────────────────┐           │   http://localhost:8000       │
    │  ResNet18 Model │           │                              │
    │  • 전이학습      │           │  ┌────────────────────────┐  │
    │  • Fine-tuning  │◄──────────┼──┤  ResNet18 Classifier   │  │
    └────────┬────────┘           │  │  best_model.pth        │  │
             │                     │  └────────────────────────┘  │
             ▼                     │                              │
    ┌─────────────────┐           │  ┌────────────────────────┐  │
    │  best_model.pth │───────────┼─►│  Grad-CAM Visualizer   │  │
    │  (76.32% acc)   │           │  └────────────────────────┘  │
    └─────────────────┘           │                              │
                                   │  ┌────────────────────────┐  │
                                   │  │  YOLO Object Detector  │  │
                                   │  │  yolo11n.pt            │  │
                                   │  └────────────────────────┘  │
                                   └──────────────────────────────┘
                                              ▲
                                              │ CUDA
                                              ▼
                                   ┌──────────────────────────────┐
                                   │      NVIDIA GPU (CUDA)       │
                                   │  • 모델 추론 가속            │
                                   │  • 85-95% GPU 사용률         │
                                   └──────────────────────────────┘
```

---

## 2. 모델 훈련 파이프라인

```
┌─────────────────────────────────────────────────────────────────┐
│                    Step 1: 데이터 다운로드                       │
└─────────────────────────────────────────────────────────────────┘

    사용자 실행: python scripts/download_dataset.py
            │
            ▼
    ┌──────────────────────┐
    │  Kaggle API 인증     │
    │  ~/.kaggle/kaggle.json│
    └──────────┬───────────┘
               │
               ▼
    ┌──────────────────────┐
    │  kagglehub.dataset   │
    │  _download()          │
    └──────────┬───────────┘
               │
               ▼
    ┌──────────────────────┐
    │  데이터셋 다운로드    │
    │  ~/.cache/kagglehub/ │
    │  /datasets/          │
    │  /food-101/          │
    └──────────┬───────────┘
               │
               ▼
    ┌──────────────────────┐
    │  dataset_path.txt    │
    │  생성 및 저장        │
    └──────────────────────┘


┌─────────────────────────────────────────────────────────────────┐
│                  Step 2: 데이터 로딩 및 전처리                   │
└─────────────────────────────────────────────────────────────────┘

    notebooks/food101_training.py 실행
            │
            ▼
    ┌────────────────────────────────────────┐
    │  ImageFolder Dataset 초기화            │
    │  • train: 75,750 images                │
    │  • test:  25,250 images                │
    └────────────┬───────────────────────────┘
                 │
                 ▼
    ┌────────────────────────────────────────┐
    │  Transform 파이프라인 생성             │
    │  ┌──────────────────────────────────┐  │
    │  │  1. Resize(256)                  │  │
    │  │  2. CenterCrop(224)              │  │
    │  │  3. ToTensor()                   │  │
    │  │  4. Normalize(                   │  │
    │  │       mean=[0.485,0.456,0.406],  │  │
    │  │       std=[0.229,0.224,0.225]    │  │
    │  │     )                             │  │
    │  └──────────────────────────────────┘  │
    └────────────┬───────────────────────────┘
                 │
                 ▼
    ┌────────────────────────────────────────┐
    │  DataLoader 설정                       │
    │  • batch_size: 128                     │
    │  • num_workers: 4                      │
    │  • pin_memory: False                   │
    │  • prefetch_factor: 2                  │
    │  • persistent_workers: True            │
    └────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────┐
│                    Step 3: 모델 구성 및 훈련                     │
└─────────────────────────────────────────────────────────────────┘

    ┌────────────────────────┐
    │  ResNet18 로드         │
    │  (ImageNet pretrained) │
    └───────────┬────────────┘
                │
                ▼
    ┌────────────────────────┐
    │  FC Layer 교체         │
    │  512 → 101 classes     │
    └───────────┬────────────┘
                │
                ▼
    ┌────────────────────────┐
    │  GPU로 모델 이동       │
    │  model.to('cuda')      │
    └───────────┬────────────┘
                │
                ▼
    ┌────────────────────────────────────────┐
    │  훈련 루프 (각 에폭마다)              │
    │                                        │
    │  FOR epoch in range(num_epochs):      │
    │    │                                   │
    │    ├─► 훈련 단계                      │
    │    │   ┌─────────────────────┐        │
    │    │   │ 1. Forward Pass     │        │
    │    │   │    images → model    │        │
    │    │   │    → logits(128,101) │        │
    │    │   └──────────┬──────────┘        │
    │    │              ▼                    │
    │    │   ┌─────────────────────┐        │
    │    │   │ 2. Loss 계산        │        │
    │    │   │    CrossEntropyLoss │        │
    │    │   └──────────┬──────────┘        │
    │    │              ▼                    │
    │    │   ┌─────────────────────┐        │
    │    │   │ 3. Backward Pass    │        │
    │    │   │    loss.backward()  │        │
    │    │   └──────────┬──────────┘        │
    │    │              ▼                    │
    │    │   ┌─────────────────────┐        │
    │    │   │ 4. Weight Update    │        │
    │    │   │    optimizer.step() │        │
    │    │   └─────────────────────┘        │
    │    │                                   │
    │    └─► 검증 단계                      │
    │        ┌─────────────────────┐        │
    │        │ with torch.no_grad()│        │
    │        │   evaluate on       │        │
    │        │   test set          │        │
    │        └──────────┬──────────┘        │
    │                   ▼                    │
    │        ┌─────────────────────┐        │
    │        │ 정확도 계산         │        │
    │        │ best model 저장     │        │
    │        └─────────────────────┘        │
    └────────────────────────────────────────┘
                │
                ▼
    ┌────────────────────────────────────────┐
    │  최종 모델 저장                        │
    │  outputs/models/best_model.pth         │
    │  {                                     │
    │    'epoch': 6,                         │
    │    'model_state_dict': {...},         │
    │    'optimizer_state_dict': {...},     │
    │    'best_acc': 76.32                  │
    │  }                                     │
    └────────────────────────────────────────┘
```

---

## 3. API 서버 시작 과정

```
┌─────────────────────────────────────────────────────────────────┐
│              API 서버 시작: python api/main.py                   │
└─────────────────────────────────────────────────────────────────┘

    ┌────────────────────────┐
    │  FastAPI 앱 초기화     │
    │  app = FastAPI(...)    │
    └───────────┬────────────┘
                │
                ▼
    ┌────────────────────────┐
    │  CORS 미들웨어 추가    │
    │  allow_origins=["*"]   │
    └───────────┬────────────┘
                │
                ▼
    ┌────────────────────────────────────────┐
    │  @app.on_event("startup")              │
    │  서버 시작 이벤트 핸들러               │
    └───────────┬────────────────────────────┘
                │
                ├─► Step 1: 디바이스 설정
                │   ┌─────────────────────────┐
                │   │ torch.cuda.is_available()│
                │   │   ✓ → DEVICE = 'cuda'    │
                │   │   ✗ → DEVICE = 'cpu'     │
                │   └─────────────────────────┘
                │
                ├─► Step 2: 클래스 로드
                │   ┌─────────────────────────┐
                │   │ load_classes()          │
                │   │ • dataset_path.txt 읽기 │
                │   │ • classes.txt 파싱      │
                │   │ → CLASSES = [101 items] │
                │   └─────────────────────────┘
                │
                ├─► Step 3: Transform 파이프라인
                │   ┌─────────────────────────┐
                │   │ transforms.Compose([    │
                │   │   Resize(256),          │
                │   │   CenterCrop(224),      │
                │   │   ToTensor(),           │
                │   │   Normalize(...)        │
                │   │ ])                      │
                │   └─────────────────────────┘
                │
                ├─► Step 4: ResNet18 모델 로드
                │   ┌─────────────────────────┐
                │   │ get_model(101)          │
                │   │ • resnet18 생성         │
                │   │ • fc layer 교체         │
                │   └──────────┬──────────────┘
                │              ▼
                │   ┌─────────────────────────┐
                │   │ 체크포인트 로드         │
                │   │ checkpoint = torch.load(│
                │   │   'best_model.pth'      │
                │   │ )                       │
                │   └──────────┬──────────────┘
                │              ▼
                │   ┌─────────────────────────┐
                │   │ 가중치 적용             │
                │   │ model.load_state_dict(  │
                │   │   checkpoint[           │
                │   │     'model_state_dict'  │
                │   │   ]                     │
                │   │ )                       │
                │   └──────────┬──────────────┘
                │              ▼
                │   ┌─────────────────────────┐
                │   │ GPU로 이동              │
                │   │ model.to(DEVICE)        │
                │   └──────────┬──────────────┘
                │              ▼
                │   ┌─────────────────────────┐
                │   │ 평가 모드 설정          │
                │   │ model.eval()            │
                │   └─────────────────────────┘
                │
                └─► Step 5: YOLO 초기화
                    ┌─────────────────────────┐
                    │ get_yolo_detector()     │
                    │ • yolo11n.pt 다운로드   │
                    │ • GPU로 모델 로드       │
                    └─────────────────────────┘
                                │
                                ▼
    ┌────────────────────────────────────────┐
    │  서버 준비 완료!                       │
    │  http://localhost:8000                 │
    │  • Swagger UI: /docs                   │
    │  • 헬스 체크: /health                  │
    └────────────────────────────────────────┘
```

---

## 4. 사용자 요청 처리 흐름

### 4.1 이미지 분류 (/predict)

```
┌─────────────────────────────────────────────────────────────────┐
│            사용자: 이미지 업로드 및 분류 요청                    │
└─────────────────────────────────────────────────────────────────┘

    브라우저                     네트워크                  FastAPI

    ┌──────────┐               ┌──────────┐            ┌──────────┐
    │ 이미지   │               │          │            │          │
    │ 선택     │               │          │            │          │
    └────┬─────┘               │          │            │          │
         │                     │          │            │          │
         ▼                     │          │            │          │
    ┌──────────────────┐      │          │            │          │
    │ FormData 생성    │      │          │            │          │
    │ file: image.jpg  │      │          │            │          │
    └────┬─────────────┘      │          │            │          │
         │                     │          │            │          │
         ▼                     │          │            │          │
    ┌──────────────────┐      │          │            │          │
    │ axios.post(      │      │          │            │          │
    │  '/predict',     │──────┼─────────►│            │          │
    │  formData        │      │ HTTP POST│            │          │
    │ )                │      │multipart │            │          │
    └──────────────────┘      │          │            │          │
                               │          ├───────────►│          │
                               │          │            │ @app.post│
                               │          │            │ ("/predict")│
                               │          │            └────┬─────┘
                               │          │                 │
                               │          │                 ▼
                               │          │     ┌────────────────────┐
                               │          │     │ 1. UploadFile 수신 │
                               │          │     │   await file.read()│
                               │          │     │   → bytes          │
                               │          │     └────────┬───────────┘
                               │          │              ▼
                               │          │     ┌────────────────────┐
                               │          │     │ 2. 이미지 디코딩   │
                               │          │     │   PIL.Image.open() │
                               │          │     │   → RGB Image      │
                               │          │     └────────┬───────────┘
                               │          │              ▼
                               │          │     ┌────────────────────┐
                               │          │     │ 3. 전처리          │
                               │          │     │   TRANSFORM(img)   │
                               │          │     │   → Tensor(3,224,  │
                               │          │     │              224)  │
                               │          │     │   .unsqueeze(0)    │
                               │          │     │   → (1,3,224,224)  │
                               │          │     └────────┬───────────┘
                               │          │              ▼
                               │          │     ┌────────────────────┐
                               │          │     │ 4. GPU 전송        │
                               │          │     │   .to(DEVICE)      │
                               │          │     └────────┬───────────┘
                               │          │              ▼
                               │          │     ┌────────────────────┐
                               │          │     │ 5. 모델 추론       │
                               │          │     │   with no_grad():  │
                               │          │     │     outputs=MODEL( │
                               │          │     │       input_tensor)│
                               │          │     │   → Logits(1, 101) │
                               │          │     └────────┬───────────┘
                               │          │              ▼
                               │          │     ┌────────────────────┐
                               │          │     │ 6. Softmax 적용    │
                               │          │     │   torch.softmax()  │
                               │          │     │   → prob(101)      │
                               │          │     └────────┬───────────┘
                               │          │              ▼
                               │          │     ┌────────────────────┐
                               │          │     │ 7. Top-5 추출      │
                               │          │     │   torch.topk(p, 5) │
                               │          │     └────────┬───────────┘
                               │          │              ▼
                               │          │     ┌────────────────────┐
                               │          │     │ 8. JSON 생성       │
                               │          │     │ {                  │
                               │          │     │   success: true,   │
                               │          │     │   prediction: {    │
                               │          │     │     class: "pizza",│
                               │          │     │     confidence:0.85 │
                               │          │     │   },               │
                               │          │     │   top5: [...]      │
                               │          │     │ }                  │
                               │          │     └────────┬───────────┘
                               │          │              │
                               │          │◄─────────────┘
                               │ HTTP 200 │
                               │ JSON     │
    ┌──────────────────┐      │          │
    │ response.data    │◄─────┼──────────┘
    │ setResult(data)  │      │
    └────┬─────────────┘      │
         │                     │
         ▼                     │
    ┌──────────────────┐      │
    │ 화면에 결과 표시  │      │
    │ • 음식 이름      │      │
    │ • 신뢰도 %       │      │
    │ • Top-5 바 차트  │      │
    └──────────────────┘      │
```

### 4.2 Grad-CAM 히트맵 (/predict/gradcam)

```
┌─────────────────────────────────────────────────────────────────┐
│          사용자: Grad-CAM 히트맵 생성 요청                       │
└─────────────────────────────────────────────────────────────────┘

    [1-5단계는 /predict와 동일]
            │
            ▼
    ┌────────────────────────────────────────┐
    │  6. Grad-CAM 생성                      │
    └────────────────────────────────────────┘
            │
            ├─► Step 6.1: GradCAM 객체 생성
            │   ┌─────────────────────────────┐
            │   │ gradcam = GradCAM(          │
            │   │   model,                    │
            │   │   target_layer=layer4[-1]   │
            │   │ )                           │
            │   └─────────────────────────────┘
            │
            ├─► Step 6.2: Forward Hook 등록
            │   ┌─────────────────────────────┐
            │   │ def forward_hook(m,i,o):    │
            │   │   activations = o.detach()  │
            │   │   # shape: (1,512,7,7)      │
            │   │                             │
            │   │ layer.register_forward_hook │
            │   │   (forward_hook)            │
            │   └─────────────────────────────┘
            │
            ├─► Step 6.3: Forward Pass
            │   ┌─────────────────────────────┐
            │   │ outputs = model(input)      │
            │   │ → Logits (1, 101)           │
            │   │ [activations 자동 저장됨]   │
            │   └─────────────────────────────┘
            │
            ├─► Step 6.4: Backward Hook 등록 & 실행
            │   ┌─────────────────────────────┐
            │   │ def backward_hook(m,gi,go): │
            │   │   gradients = go[0].detach()│
            │   │   # shape: (1,512,7,7)      │
            │   │                             │
            │   │ layer.register_full_        │
            │   │   backward_hook(...)        │
            │   │                             │
            │   │ model.zero_grad()           │
            │   │ class_score = outputs[0,    │
            │   │   target_class]             │
            │   │ class_score.backward()      │
            │   │ [gradients 자동 저장됨]     │
            │   └─────────────────────────────┘
            │
            ├─► Step 6.5: CAM 계산
            │   ┌─────────────────────────────┐
            │   │ # 채널별 가중치 계산        │
            │   │ weights = gradients.mean(   │
            │   │   dim=[2,3])                │
            │   │ # → (1, 512, 1, 1)          │
            │   │                             │
            │   │ # 가중 합산                 │
            │   │ cam = (weights *            │
            │   │        activations).sum(1)  │
            │   │ # → (1, 7, 7)               │
            │   │                             │
            │   │ # ReLU & 정규화             │
            │   │ cam = F.relu(cam)           │
            │   │ cam = normalize(cam)        │
            │   │ # → [0, 1] 범위             │
            │   └─────────────────────────────┘
            │
            ├─► Step 6.6: 히트맵 생성
            │   ┌─────────────────────────────┐
            │   │ # 원본 크기로 리사이즈       │
            │   │ cam_resized = cv2.resize(   │
            │   │   cam, (224, 224))          │
            │   │                             │
            │   │ # 컬러맵 적용 (JET)         │
            │   │ heatmap = cv2.applyColorMap(│
            │   │   cam_uint8, COLORMAP_JET)  │
            │   │                             │
            │   │ # 원본 이미지와 오버레이    │
            │   │ overlay = (                 │
            │   │   heatmap * 0.4 +           │
            │   │   original * 0.6            │
            │   │ )                           │
            │   └─────────────────────────────┘
            │
            └─► Step 6.7: Base64 인코딩
                ┌─────────────────────────────┐
                │ buffered = BytesIO()        │
                │ overlay_img.save(           │
                │   buffered, format="PNG")   │
                │                             │
                │ img_str = base64.b64encode( │
                │   buffered.getvalue())      │
                │                             │
                │ data_url = "data:image/png; │
                │   base64," + img_str        │
                └─────────────────────────────┘
            │
            ▼
    ┌────────────────────────────────────────┐
    │  7. JSON 응답                          │
    │  {                                     │
    │    success: true,                      │
    │    prediction: {...},                  │
    │    top5: [...],                        │
    │    gradcam: {                          │
    │      heatmap_image: "data:image/png;   │
    │        base64,iVBORw0KGgo...",         │
    │      description: "빨간색 영역이       │
    │        AI가 중요하게 본 부분"          │
    │    }                                   │
    │  }                                     │
    └────────────────────────────────────────┘
```

### 4.3 객체 탐지 (/detect)

```
┌─────────────────────────────────────────────────────────────────┐
│             사용자: YOLO 객체 탐지 요청                          │
└─────────────────────────────────────────────────────────────────┘

    POST /detect
        │
        ▼
    ┌────────────────────────────────────────┐
    │  1. 이미지 로드 및 변환                │
    │  PIL Image → numpy array               │
    └────────────┬───────────────────────────┘
                 │
                 ▼
    ┌────────────────────────────────────────┐
    │  2. YOLO 추론                          │
    │  detector = get_yolo_detector()        │
    │  results = model.predict(              │
    │    source=image_array,                 │
    │    conf=0.25,                          │
    │    verbose=False                       │
    │  )                                     │
    └────────────┬───────────────────────────┘
                 │
                 ├─► YOLO 내부 처리
                 │   ┌─────────────────────────┐
                 │   │ • Letterbox resize      │
                 │   │   → (640, 640)          │
                 │   │ • Normalize [0,1]       │
                 │   │ • Tensor 변환           │
                 │   └───────┬─────────────────┘
                 │           ▼
                 │   ┌─────────────────────────┐
                 │   │ YOLO11n Forward Pass    │
                 │   │ → Detections            │
                 │   │   [(x1,y1,x2,y2,        │
                 │   │     conf,cls), ...]     │
                 │   └───────┬─────────────────┘
                 │           ▼
                 │   ┌─────────────────────────┐
                 │   │ NMS (비최대 억제)       │
                 │   │ • 중복 박스 제거        │
                 │   │ • IoU threshold: 0.45   │
                 │   └─────────────────────────┘
                 │
                 ▼
    ┌────────────────────────────────────────┐
    │  3. 결과 추출                          │
    │  boxes = result.boxes.xyxy  # (N, 4)  │
    │  confs = result.boxes.conf  # (N,)    │
    │  clses = result.boxes.cls   # (N,)    │
    └────────────┬───────────────────────────┘
                 │
                 ▼
    ┌────────────────────────────────────────┐
    │  4. 어노테이션 이미지 생성             │
    │  annotated = result.plot()             │
    │  • 바운딩 박스 그리기                  │
    │  • 클래스 레이블 표시                  │
    │  • 신뢰도 % 표시                       │
    └────────────┬───────────────────────────┘
                 │
                 ▼
    ┌────────────────────────────────────────┐
    │  5. Base64 인코딩                      │
    │  BGR → RGB 변환                        │
    │  → PIL Image                           │
    │  → Base64 string                       │
    └────────────┬───────────────────────────┘
                 │
                 ▼
    ┌────────────────────────────────────────┐
    │  6. JSON 응답                          │
    │  {                                     │
    │    success: true,                      │
    │    num_objects: 3,                     │
    │    detections: [                       │
    │      {                                 │
    │        class: "person",                │
    │        confidence: 0.85,               │
    │        bbox: {                         │
    │          x1: 100, y1: 150,             │
    │          x2: 300, y2: 500,             │
    │          width: 200, height: 350       │
    │        }                               │
    │      },                                │
    │      ...                               │
    │    ],                                  │
    │    annotated_image: "data:image/png;   │
    │      base64,iVBORw0KGgo..."            │
    │  }                                     │
    └────────────────────────────────────────┘
```

---

## 5. 프론트엔드 상호작용

```
┌─────────────────────────────────────────────────────────────────┐
│                   React 프론트엔드 흐름도                        │
└─────────────────────────────────────────────────────────────────┘

    ┌────────────────────┐
    │  사용자 액션       │
    └────────────────────┘
            │
            ├─► 드래그 & 드롭
            ├─► 파일 선택 클릭
            └─► Ctrl+V 붙여넣기
            │
            ▼
    ┌────────────────────────────────────────┐
    │  handleFileSelect(file)                │
    └────────────┬───────────────────────────┘
                 │
                 ├─► 검증
                 │   ┌─────────────────────────┐
                 │   │ file.type.startsWith(   │
                 │   │   'image/')             │
                 │   │ ? 계속 : 에러 표시      │
                 │   └─────────────────────────┘
                 │
                 ├─► State 업데이트
                 │   ┌─────────────────────────┐
                 │   │ setImage(file)          │
                 │   │ // File 객체 저장       │
                 │   └─────────────────────────┘
                 │
                 └─► 미리보기 생성
                     ┌─────────────────────────┐
                     │ FileReader.             │
                     │   readAsDataURL(file)   │
                     │                         │
                     │ onload: (e) => {        │
                     │   setPreview(           │
                     │     e.target.result)    │
                     │   // "data:image/jpeg;  │
                     │   //  base64,/9j/..."   │
                     │ }                       │
                     └─────────────────────────┘
            │
            ▼
    ┌────────────────────────────────────────┐
    │  UI 렌더링                             │
    │  <img src={preview} />                 │
    │  [분석하기] 버튼 활성화                │
    └────────────────────────────────────────┘
            │
            ▼ (사용자가 버튼 클릭)
    ┌────────────────────────────────────────┐
    │  handlePredict()                       │
    └────────────┬───────────────────────────┘
                 │
                 ├─► 로딩 상태
                 │   ┌─────────────────────────┐
                 │   │ setLoading(true)        │
                 │   │ // 스피너 표시          │
                 │   └─────────────────────────┘
                 │
                 ├─► 엔드포인트 결정
                 │   ┌─────────────────────────┐
                 │   │ if (mode === 'detect')  │
                 │   │   endpoint = '/detect'  │
                 │   │ else if (showGradCAM)   │
                 │   │   endpoint = '/predict/ │
                 │   │     gradcam'            │
                 │   │ else                    │
                 │   │   endpoint = '/predict' │
                 │   └─────────────────────────┘
                 │
                 ├─► FormData 생성
                 │   ┌─────────────────────────┐
                 │   │ const formData =        │
                 │   │   new FormData()        │
                 │   │ formData.append(        │
                 │   │   'file', image)        │
                 │   └─────────────────────────┘
                 │
                 ├─► axios POST 요청
                 │   ┌─────────────────────────┐
                 │   │ const response =        │
                 │   │   await axios.post(     │
                 │   │     API_URL + endpoint, │
                 │   │     formData,           │
                 │   │     {                   │
                 │   │       headers: {        │
                 │   │         'Content-Type': │
                 │   │         'multipart/     │
                 │   │          form-data'     │
                 │   │       }                 │
                 │   │     }                   │
                 │   │   )                     │
                 │   └─────────────────────────┘
                 │         │
                 │         ▼ (2-3초 대기)
                 │   ┌─────────────────────────┐
                 │   │ 서버 응답 수신          │
                 │   │ response.data = {       │
                 │   │   success: true,        │
                 │   │   prediction: {...},    │
                 │   │   top5: [...],          │
                 │   │   gradcam?: {...}       │
                 │   │ }                       │
                 │   └─────────────────────────┘
                 │
                 ├─► State 업데이트
                 │   ┌─────────────────────────┐
                 │   │ setResult(response.data)│
                 │   │ setLoading(false)       │
                 │   └─────────────────────────┘
                 │
                 └─► 에러 처리
                     ┌─────────────────────────┐
                     │ catch (error) {         │
                     │   setError(             │
                     │     error.message)      │
                     │   setLoading(false)     │
                     │ }                       │
                     └─────────────────────────┘
            │
            ▼
    ┌────────────────────────────────────────┐
    │  결과 렌더링                           │
    │  ┌──────────────────────────────────┐  │
    │  │ 분류 모드:                       │  │
    │  │ • 예측 클래스명                  │  │
    │  │ • 신뢰도 % (대형 표시)           │  │
    │  │ • Top-5 바 차트                  │  │
    │  │ • Grad-CAM 히트맵 (선택 시)      │  │
    │  └──────────────────────────────────┘  │
    │  ┌──────────────────────────────────┐  │
    │  │ 탐지 모드:                       │  │
    │  │ • 어노테이션 이미지              │  │
    │  │ • 탐지된 객체 수                 │  │
    │  │ • 각 객체 정보 카드              │  │
    │  │   - 클래스명                     │  │
    │  │   - 신뢰도 %                     │  │
    │  │   - 바운딩 박스 좌표             │  │
    │  └──────────────────────────────────┘  │
    └────────────────────────────────────────┘
```

---

## 6. 데이터 변환 체인

### 6.1 훈련 시 데이터 변환

```
┌─────────────────────────────────────────────────────────────────┐
│                     훈련 시 데이터 변환 체인                     │
└─────────────────────────────────────────────────────────────────┘

    📁 apple_pie/1001.jpg (1.2MB, JPEG, 1024x768)
        │
        ▼
    ┌────────────────────────┐
    │ PIL.Image.open()       │
    │ Image Mode: RGB        │
    │ Size: (1024, 768)      │
    └───────────┬────────────┘
                │
                ▼
    ┌────────────────────────┐
    │ transforms.Resize(256) │
    │ Size: (256, 192)       │
    │ [aspect ratio 유지]    │
    └───────────┬────────────┘
                │
                ▼
    ┌────────────────────────┐
    │ CenterCrop(224)        │
    │ Size: (224, 192) ?     │
    │ → (224, 224)로 크롭    │
    └───────────┬────────────┘
                │
                ▼
    ┌────────────────────────┐
    │ transforms.ToTensor()  │
    │ Tensor(3, 224, 224)    │
    │ dtype: float32         │
    │ range: [0.0, 1.0]      │
    └───────────┬────────────┘
                │
                ▼
    ┌────────────────────────────────────┐
    │ transforms.Normalize(              │
    │   mean=[0.485, 0.456, 0.406],      │
    │   std=[0.229, 0.224, 0.225]        │
    │ )                                  │
    │ 각 채널별 정규화:                  │
    │ output[c] = (input[c] - mean[c])   │
    │             / std[c]               │
    └───────────┬────────────────────────┘
                │
                ▼
    ┌────────────────────────┐
    │ DataLoader 배치 생성   │
    │ Tensor(128, 3, 224,    │
    │        224)             │
    └───────────┬────────────┘
                │
                ▼
    ┌────────────────────────┐
    │ .to('cuda')            │
    │ GPU Memory에 복사      │
    │ CUDA Tensor(128, 3,    │
    │   224, 224)            │
    └───────────┬────────────┘
                │
                ▼
    ┌────────────────────────────────┐
    │ ResNet18 Forward Pass          │
    │                                │
    │ Conv1 (7x7, stride=2)          │
    │ → (128, 64, 112, 112)          │
    │                                │
    │ MaxPool (3x3, stride=2)        │
    │ → (128, 64, 56, 56)            │
    │                                │
    │ Layer1 (BasicBlock x 2)        │
    │ → (128, 64, 56, 56)            │
    │                                │
    │ Layer2 (BasicBlock x 2)        │
    │ → (128, 128, 28, 28)           │
    │                                │
    │ Layer3 (BasicBlock x 2)        │
    │ → (128, 256, 14, 14)           │
    │                                │
    │ Layer4 (BasicBlock x 2)        │
    │ → (128, 512, 7, 7)             │
    │                                │
    │ AdaptiveAvgPool2d              │
    │ → (128, 512, 1, 1)             │
    │                                │
    │ Flatten                        │
    │ → (128, 512)                   │
    │                                │
    │ FC Layer (512 → 101)           │
    │ → (128, 101)                   │
    │                                │
    │ [Logits - 훈련 모드]           │
    └───────────┬────────────────────┘
                │
                ▼
    ┌────────────────────────┐
    │ CrossEntropyLoss       │
    │ input: (128, 101)      │
    │ target: (128,)         │
    │ → scalar loss          │
    └───────────┬────────────┘
                │
                ▼
    ┌────────────────────────┐
    │ Backward Pass          │
    │ loss.backward()        │
    │ → Gradients 계산       │
    └───────────┬────────────┘
                │
                ▼
    ┌────────────────────────┐
    │ Optimizer Step         │
    │ Adam.step()            │
    │ → 가중치 업데이트      │
    └────────────────────────┘
```

### 6.2 추론 시 데이터 변환

```
┌─────────────────────────────────────────────────────────────────┐
│                     추론 시 데이터 변환 체인                     │
└─────────────────────────────────────────────────────────────────┘

    📷 사용자 업로드 (pizza.jpg, 245KB, JPEG)
        │
        ▼
    ┌────────────────────────────────────┐
    │ 브라우저: File 객체                │
    │ type: 'image/jpeg'                 │
    │ size: 251234 bytes                 │
    └───────────┬────────────────────────┘
                │
                ▼
    ┌────────────────────────────────────┐
    │ FileReader.readAsDataURL()         │
    │ → Data URL (base64)                │
    │ "data:image/jpeg;base64,           │
    │  /9j/4AAQSkZJRgABA..."             │
    └───────────┬────────────────────────┘
                │ [미리보기 표시]
                │
                ▼
    ┌────────────────────────────────────┐
    │ FormData.append('file', file)      │
    │ → multipart/form-data              │
    └───────────┬────────────────────────┘
                │
                ▼
    ┌────────────────────────────────────┐
    │ HTTP POST → FastAPI 서버           │
    │ Content-Type:                      │
    │   multipart/form-data;             │
    │   boundary=----WebKit...           │
    └───────────┬────────────────────────┘
                │
                ▼
    ┌────────────────────────────────────┐
    │ await file.read()                  │
    │ → bytes (바이너리 데이터)          │
    │ len: 251234                        │
    └───────────┬────────────────────────┘
                │
                ▼
    ┌────────────────────────────────────┐
    │ PIL.Image.open(BytesIO(bytes))     │
    │ → PIL Image                        │
    │ Mode: RGB                          │
    │ Size: (800, 600)                   │
    └───────────┬────────────────────────┘
                │
                ▼
    ┌────────────────────────────────────┐
    │ TRANSFORM(image)                   │
    │ • Resize(256) → (341, 256)         │
    │ • CenterCrop(224) → (224, 224)     │
    │ • ToTensor() → (3, 224, 224)       │
    │ • Normalize() → 표준화             │
    └───────────┬────────────────────────┘
                │
                ▼
    ┌────────────────────────────────────┐
    │ .unsqueeze(0)                      │
    │ (3, 224, 224) → (1, 3, 224, 224)   │
    └───────────┬────────────────────────┘
                │
                ▼
    ┌────────────────────────────────────┐
    │ .to(DEVICE)                        │
    │ → GPU Tensor(1, 3, 224, 224)       │
    └───────────┬────────────────────────┘
                │
                ▼
    ┌────────────────────────────────────┐
    │ with torch.no_grad():              │
    │   outputs = MODEL(input_tensor)    │
    │ → Logits(1, 101)                   │
    │                                    │
    │ [예시 출력]                        │
    │ [-2.1, 0.5, ..., 5.2, ..., 1.3]    │
    │          pizza=5.2 (최대값)        │
    └───────────┬────────────────────────┘
                │
                ▼
    ┌────────────────────────────────────┐
    │ torch.softmax(outputs, dim=1)      │
    │ → Probabilities(101,)              │
    │                                    │
    │ [예시 출력]                        │
    │ [0.001, 0.003, ..., 0.852, ...]    │
    │          pizza=0.852 (85.2%)       │
    │                                    │
    │ 수학: P(i) = exp(logit_i) /        │
    │              Σ exp(logit_j)        │
    └───────────┬────────────────────────┘
                │
                ▼
    ┌────────────────────────────────────┐
    │ torch.topk(probabilities, 5)       │
    │ → top5_values, top5_indices        │
    │                                    │
    │ [예시 출력]                        │
    │ values: [0.852, 0.073, 0.041,      │
    │          0.018, 0.009]             │
    │ indices: [53, 72, 14, 89, 30]      │
    └───────────┬────────────────────────┘
                │
                ▼
    ┌────────────────────────────────────┐
    │ .cpu().item()                      │
    │ GPU Tensor → Python float          │
    │ 0.8523 (85.23%)                    │
    └───────────┬────────────────────────┘
                │
                ▼
    ┌────────────────────────────────────┐
    │ JSON 직렬화                        │
    │ {                                  │
    │   "success": true,                 │
    │   "prediction": {                  │
    │     "class": "pizza",              │
    │     "class_id": 53,                │
    │     "confidence": 0.8523,          │
    │     "confidence_percent": "85.23%" │
    │   },                               │
    │   "top5": [                        │
    │     {                              │
    │       "rank": 1,                   │
    │       "class": "pizza",            │
    │       "confidence": 0.8523         │
    │     },                             │
    │     ...                            │
    │   ]                                │
    │ }                                  │
    └───────────┬────────────────────────┘
                │
                ▼
    ┌────────────────────────────────────┐
    │ HTTP Response                      │
    │ HTTP/1.1 200 OK                    │
    │ Content-Type: application/json     │
    └───────────┬────────────────────────┘
                │
                ▼
    ┌────────────────────────────────────┐
    │ axios 파싱                         │
    │ → JavaScript 객체                  │
    └───────────┬────────────────────────┘
                │
                ▼
    ┌────────────────────────────────────┐
    │ setResult(response.data)           │
    │ React State 업데이트               │
    └───────────┬────────────────────────┘
                │
                ▼
    ┌────────────────────────────────────┐
    │ 화면 렌더링                        │
    │ ┌────────────────────────────────┐ │
    │ │  🍕 Pizza                      │ │
    │ │  신뢰도: 85.23%                │ │
    │ │                                │ │
    │ │  Top 5 예측:                   │ │
    │ │  1. Pizza ████████████ 85.23%  │ │
    │ │  2. Taco ███ 7.3%              │ │
    │ │  3. ...                        │ │
    │ └────────────────────────────────┘ │
    └────────────────────────────────────┘
```

---

## 📌 핵심 포인트 요약

### 메모리 위치별 데이터 저장

| 위치 | 저장 데이터 | 크기 |
|------|------------|------|
| **디스크** | Food-101 데이터셋 | 5GB |
| | best_model.pth | 129MB |
| **CPU RAM** | 데이터 로더 버퍼 | 1.5GB |
| | FastAPI 프로세스 | 200MB |
| **GPU VRAM** | 모델 파라미터 | 45MB |
| | 입력 배치 | 변동 |
| | Activations | 200MB |
| **브라우저** | React 앱 | 10MB |
| | 이미지 미리보기 | 1-3MB |

### 통신 프로토콜별 데이터 형식

| 구간 | 프로토콜 | 데이터 형식 |
|------|---------|-----------|
| 학습 → 디스크 | PyTorch save/load | Pickle (best_model.pth) |
| 클라이언트 ↔ 서버 | HTTP/REST | JSON, multipart/form-data |
| 서버 ↔ 모델 | Python 함수 | PyTorch Tensor (in-memory) |
| 서버 ↔ GPU | CUDA | CUDA Tensor |
| 서버 → 클라이언트 (이미지) | HTTP | Base64 Data URL |

### 처리 시간 (예상)

| 작업 | 시간 |
|------|------|
| 이미지 업로드 (클라이언트 → 서버) | 50-200ms |
| 이미지 전처리 | 10-20ms |
| ResNet18 추론 (단일) | 20-50ms |
| Grad-CAM 생성 | 50-100ms |
| YOLO 추론 | 30-80ms |
| JSON 직렬화 및 응답 | 10-20ms |
| **전체 요청 처리** | **~100-300ms** |

---

## 관련 문서

- [README.md](README.md) - 프로젝트 소개 및 시작 가이드
- [DATA_FLOW.md](DATA_FLOW.md) - 데이터 흐름 상세 설명
- [PROJECT_STRUCTURE.md](PROJECT_STRUCTURE.md) - 프로젝트 구조
- [DEPLOYMENT.md](DEPLOYMENT.md) - 배포 가이드
